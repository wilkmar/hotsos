checks:
  has_1883089:
    input:
      path: 'var/log/neutron/neutron-l3-agent.log'
    expr: '([\d-]+) [\d:]+\.\d{3} .+AttributeError: ''NoneType'' object has no attribute ''get'''
    hint: 'AttributeError'
conclusions:
  lp1883089:
    decision: has_1883089
    raises:
      type: LaunchpadBug
      bug-id: 1883089
      message: >-
        The neutron l3-agent on this host is affected by known bug LP1883089,
        which breaks DVR floating IP connectivity. Upgrade neutron to the
        latest version; see the bug for details.
