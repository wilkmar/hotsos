checks:
  isolcpus_enabled:
    property: hotsos.core.plugins.kernel.KernelBase.isolcpus_enabled
  has_1907686:
    input:
      path: 'var/log/neutron/neutron-openvswitch-agent.log'
    expr: '([\d-]+) [\d:]+\.\d{3} .+OVS database connection to OVN_\S+bound failed with error: ''Timeout''.+'
conclusions:
  lp1907686:
    decision:
      - isolcpus_enabled
      - has_1907686
    raises:
      type: LaunchpadBug
      bug-id: 1907686
      message: >-
        neutron-ovn on this host is affected by known bug LP1907686, which
        disrupts OVS/OVN database connections. Upgrade openvswitch to the latest
        version; see the bug for details.
