checks:
  has_1929832:
    input:
      path: 'var/log/neutron/neutron-l3-agent.log'
    expr: '([\d-]+) [\d:]+\.\d{3} .+Error while deleting router \S+: \S+ProcessExecutionError: .+ /usr/bin/neutron-rootwrap: Unauthorized command: kill -15 \d+ \(no filter matched\)'
conclusions:
  lp1929832:
    decision: has_1929832
    raises:
      type: LaunchpadBug
      bug-id: 1929832
      message: >-
        The neutron l3-agent on this host is affected by known bug LP1929832,
        which prevents neutron routers from being deleted correctly. Upgrade
        neutron to the latest version; see the bug for details.
